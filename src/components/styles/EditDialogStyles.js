import styled from 'styled-components';

export const EditDialogStyles = styled.div`
  border-left: 2px solid var(--yellow);
  font-size: 1.5rem;
  padding: 1rem 2rem;
  margin: 3rem 0;
  background: var(--surface-medium);
  p:first-child {
    margin-top: 0;
  }
`;
